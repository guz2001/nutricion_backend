# Dependencias de NutriConsulta backend — Python 3.9
# Instalar con:  pip install -r requirements.txt
#
# Ojo: este archivo lista SOLO lo que el proyecto necesita. Si se regenera con
# `pip freeze`, hay que hacerlo con el venv activado, nunca con el Python del
# sistema, o se cuelan paquetes de Fedora que nadie más puede instalar.

# Framework
Django==4.2.30
djangorestframework==3.16.1

# Driver de PostgreSQL
psycopg2-binary==2.9.12

# Variables de entorno desde .env
python-decouple==3.8

# Filtros de query params y CORS para el frontend Astro
django-filter==25.1
django-cors-headers==4.9.0

# Dependencias transitivas de Django, fijadas para instalaciones reproducibles
asgiref==3.11.1
sqlparse==0.5.5
typing_extensions==4.16.0
